#include <stdio.h>
#include <string.h>
#include "cal.h"

void getDays(char *buffer, int mon, int year)
{
    // Use this pattern to help your write to the buffer:
    int char_out_count = 0;
    char_out_count += sprintf(buffer + char_out_count, "TODO");
}